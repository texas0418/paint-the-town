name: Promotion checks (main)

on:
  pull_request:
    branches: [main]

jobs:
  quality:
    name: quality
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Typecheck
        run: npx tsc --noEmit || true  # TODO(#4): make blocking once legacy type errors are fixed
      - name: Lint (incl. complexity limits)
        run: npx eslint .
      - name: Pure-module tests
        run: npm test
      - name: LLM slop check
        run: bash scripts/ci/slop-check.sh
      - name: Expo doctor (dependency/config health)
        run: npx expo-doctor
        # Advisory, not blocking: this project builds with raw xcodebuild (not
        # expo prebuild), so the non-CNG app.json warning is a false positive
        # here, and the babel-preset-expo/jest version drift is a tracked
        # follow-up rather than a merge blocker. See TODO(#7).
        continue-on-error: true

  secrets:
    name: secrets
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
